<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />

    <title><%= content_for?(:title) ? yield(:title) : "JOAHH" %></title>
    
    <%= stylesheet_link_tag    "application" %>
    <%= javascript_include_tag "application", 'data-turbolinks-track' => true %>
    <%= csrf_meta_tags %>
  </head>

  <body>

    <nav class="navbar navbar-inverse">
          <div class="container-fluid">
            <div class="navbar-header">
              <a class="navbar-brand" href="/">JOAHH</a>
            </div>
            <div class="nav navbar-nav">
          
            </div>
            <div class="nav navbar-nav navbar-right">
             <% if logged_in? %>
             <% if current_user.avatar? %>
               <%= image_tag current_user.avatar.profile.url, :class => "img-responsive img-circle", :style => "margin-bottom: 3px; margin-right: 15px;" %>
             <% else %> 
               <img src="..." alt="...">
             <%end%>
              <li><a href="/users/<%=current_user.id%>"><span class="glyphicon glyphicon-user"></span><%= current_user.full_name %></a></li>
               <li class="cart-text"><%= render 'layouts/cart_text' %></li>
               <li><a href="#" class="glyphicon glyphicon-log-out"><%= link_to 'Logout', sign_out_path, method: :delete %></a></li>
              <% else %>
              <li><a href="#"><span class="glyphicon glyphicon-user"></span> <%= link_to 'Login', '/sign_in' %></a></li>
              <li><a href="#"><span class="glyphicon glyphicon-log-in"></span><%= link_to 'Signup', '/sign_up' %></a></li>
              <% end %>
            </div>
          </div>
        </nav>

        <div class="container">
          <%= flash_messages %>
          <%= yield %>
        </div>

  </body>
</html>
